# Оценка мощности A/B-теста методом Монте-Карло

Проект для оценки того, насколько A/B-тест способен обнаружить эффект от нового рекомендательного алгоритма в ленте контента.  
Метод: моделирование пользовательского поведения и эксперимента методом Монте-Карло на основе исторических данных.

---

## 🎯 Цель проекта

В продукте тестируется новый алгоритм рекомендаций, который:

- показывает пользователю дополнительные **1–2 поста**;
- срабатывает с вероятностью **90%**;
- применяется только к достаточно активным пользователям (по числу просмотров).

**Задачи проекта:**

- оценить мощность A/B-теста (вероятность обнаружить реальный эффект) при разных настройках эксперимента;
- сформулировать рекомендации по дизайну эксперимента: длительность, таргет, критерий анализа.

---

## 🧱 Данные и технологии

**Исходные данные (лог событий ленты):**

- атомарные события пользователей:
  - `user_id`
  - `time`
  - `action` (`view` / `like`)

**Технологический стек:**

- Python 3.10+
- numpy, pandas  
- scipy (Welch t-test)  
- tqdm (прогресс-бар)  
- pandahouse (подключение к ClickHouse)  
- ClickHouse как источник исторических данных

**Используемые агрегаты:**

- недельные просмотры на пользователя;
- дневной CTR пользователя (`likes / views` по паре `user_id × day`).

---

## 🔬 Методология

### 1. Построение эмпирических распределений

На историческом периоде (A/A-тест) считаются:

#### 1.1. Недельные просмотры на пользователя

```python
# агрегирование по user_id
df_week = (
    df
    .groupby('user_id', as_index=False)
    .agg({'views': 'sum', 'likes': 'sum'})
)

views_per_user = df_week['views'].to_numpy(dtype=int)
```

#### 1.2. Дневной CTR пользователя

```python
df['ctr_day'] = df['likes'] / df['views']
ctr_per_user_day = df['ctr_day'].to_numpy(float)
```

Эти распределения используются как «генеральная совокупность» в Монте-Карло-симуляциях.

---

### 2. Модель эксперимента

Для каждого сценария моделируется полный цикл A/B-теста: формирование групп, применение эффекта, генерация лайков, статистический тест.

#### 2.1. Сэмплирование пользователей

```python
views_A = rng.choice(views_per_user, size=n_group, replace=True)
views_B = rng.choice(views_per_user, size=n_group, replace=True)

ctr_A = rng.choice(ctr_per_user_day, size=n_group, replace=True)
ctr_B = rng.choice(ctr_per_user_day, size=n_group, replace=True)
```

- `views_A`, `views_B` — недельные просмотры на пользователя для групп A и B;
- `ctr_A`, `ctr_B` — дневной CTR пользователя, смоделированный из эмпирического распределения.

#### 2.2. Эффект рекомендательного алгоритма (группа B)

```python
extra_views_base = 1 + rng.binomial(n=1, p=0.5, size=n_group)   # +1 или +2 просмотра
algo_trigger     = rng.binomial(n=1, p=0.9, size=n_group)       # сработал / не сработал
eligible         = (views_B >= threshold).astype(int)           # порог по просмотрам

extra_views = extra_views_base * algo_trigger * eligible
new_views_B = views_B + extra_views
```

- `threshold` — порог по числу просмотров (30 или 50 в зависимости от сценария);
- если пользователь не проходит порог или алгоритм не сработал, добавка просмотров равна 0;
- если проходит порог и алгоритм сработал — пользователь получает +1 или +2 просмотра.

#### 2.3. Генерация лайков

```python
likes_A = rng.binomial(n=views_A,     p=ctr_A)
likes_B = rng.binomial(n=new_views_B, p=ctr_B)
```

- лайки моделируются как биномиальное распределение:
  - `n` — число просмотров пользователя,
  - `p` — CTR пользователя.

#### 2.4. t-тест Уэлча по лайкам на пользователя

```python
from scipy.stats import ttest_ind

stat, p = ttest_ind(likes_B, likes_A, equal_var=False)
```

- используется t-тест Уэлча, так как дисперсии в группах могут отличаться;
- уровень значимости фиксирован: α = 0.05;
- симуляция повторяется `N = 20_000` раз, мощность = доля симуляций, где `p < 0.05`.

---

## 3. Сценарии эксперимента

Моделируются четыре основных сценария:

1. 1 неделя, порог активности 50 просмотров, все пользователи в анализе;  
2. 1 неделя, порог 30 просмотров, все пользователи в анализе;  
3. 2 недели, порог 30 просмотров, все пользователи в анализе;  
4. 2 недели, порог 30 просмотров, анализ только пользователей с ≥30 просмотров.

Во всех сценариях:

- пользователи делятся 50/50 на группы A и B;
- уровень значимости α = 0.05;
- оценка мощности через Монте-Карло.

Для сценария 4 дополнительно используется фильтрация только активных пользователей при расчёте t-теста:

```python
mask_A = views_A >= 30
mask_B = views_B >= 30

stat, p = ttest_ind(likes_B[mask_B], likes_A[mask_A], equal_var=False)
```

---

## 📊 Результаты

При α = 0.05 получены следующие оценки мощности t-теста (по лайкам на пользователя):

1. 1 неделя, порог 50, все пользователи — мощность ≈ **26.3%**  
2. 1 неделя, порог 30, все пользователи — мощность ≈ **42.7%**  
3. 2 недели, порог 30, все пользователи — мощность ≈ **57.2%**  
4. 2 недели, порог 30, только пользователи с ≥30 просмотров — мощность ≈ **65.2%**

---

## 🧠 Интерпретация и выводы

### 1. Короткий эксперимент (1 неделя) даёт слишком низкую мощность

- при пороге 50 мощность около 26% — тест почти всегда не видит эффект;
- при пороге 30 мощность растёт до ~43%, но всё ещё далека от комфортных значений.

**Вывод:** решение «эффекта нет» по результатам 1-недельного теста статистически слабо.

### 2. Снижение порога активности с 50 до 30 увеличивает детектируемый эффект

- алгоритм начинает затрагивать больше пользователей;
- средний эффект по популяции растёт, что повышает мощность теста даже при той же длительности.

### 3. Увеличение длительности до 2 недель заметно повышает чувствительность

- при пороге 30 и длительности 2 недели мощность достигает ~57%;
- это лучше, но всё ещё ниже классических 80%, которые обычно хотят видеть продукт и аналитики.

### 4. Таргетирование только на релевантных пользователей сильно помогает

- анализ по всем пользователям размывает эффект за счёт тех, кого алгоритм не затрагивает;
- фильтрация пользователей с ≥30 просмотров повышает мощность до ~65.2% без изменения длительности теста и статистического критерия.

**Вывод:** корректный выбор целевой популяции (eligibility-критериев) — один из самых сильных рычагов повышения мощности A/B-теста.

### 5. Монте-Карло на исторических данных — рабочий инструмент планирования экспериментов

- позволяет ещё до запуска оценить ожидаемую мощность;
- помогает подобрать длительность эксперимента и целевой сегмент;
- даёт аргументы для согласования дизайна теста с продуктовой и ML-командами.

---

## 🚀 Как запустить

1. Установить зависимости:
```bash
pip install -r requirements.txt
```
2. При необходимости настроить подключение к ClickHouse в `src/ab_test_power_monte_carlo.py` (словарь `connection`).
3. Запустить скрипт:
```bash
python src/ab_test_power_monte_carlo.py
```

Скрипт выгружает необходимые агрегаты из базы, выполняет Монте-Карло-симуляции и выводит в консоль оценку мощности для заданных сценариев.
